#!/usr/bin/env bash

yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa

yabai -m config focus_follows_mouse autofocus

yabai -m signal --add event=window_created action="~/.config/yabai/window_created.sh"
