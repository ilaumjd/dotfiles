#!/bin/bash

# Source unified color palette
source ~/.config/_vars/colors.conf

options=(
	style=round
	width=6.0
	hidpi=off
	active_color="$ACCENT_BLUE"
	inactive_color="$TEXT_MUTED"
)

borders "${options[@]}"
