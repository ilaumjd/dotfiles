#!/bin/bash

# Source unified color palette
source ~/.config/_vars/colors.sh

options=(
	style=round
	active_color="$ACCENT_PRIMARY"
	inactive_color="$TEXT_INACTIVE"
	width=6.0
	hidpi=off
	ax_focus=on
	blacklist="Simulator"
)

borders "${options[@]}"
